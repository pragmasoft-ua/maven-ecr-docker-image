# maven-ecr-docker-image
Docker image derived from maven official image with the ecr-login docker credentials helper installed, to be used with jib maven plugin to push images to ecr docker repository
